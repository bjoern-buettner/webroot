{% for host in virtualhosts %}
    <VirtualHost *:80>
        {% include 'http-virtualhost.twig' %}

        RewriteCond %{HTTPS} !=on
        RewriteRule (.*) https://{{ host.vhost }}$1 [R,L]
    </VirtualHost>
{% endfor %}
{% for host in defaulthosts %}
    <VirtualHost *:80>
        {% include 'http-virtualhost.twig' %}

        SSLEngine Off
        RewriteEngine On
        RewriteRule (.*) https://{{ host.domain }}$1 [R,L]
    </VirtualHost>
{% endfor %}
