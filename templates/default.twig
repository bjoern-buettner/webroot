<VirtualHost *:80>
    {% include 'http-virtualhost.twig' %}

    RewriteCond %{HTTPS} !=on
    RewriteRule (.*) https://{{ host.vhost }}$1 [R,L]
</VirtualHost>
<VirtualHost *:443>
    {% set domain=host.domain %}
    {% include 'https-virtualhost.twig' %}
</VirtualHost>