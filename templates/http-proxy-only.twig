{% for host in virtualhosts %}
    <VirtualHost *:80>
        {% include 'http-proxy.twig' %}

        RewriteEngine On
        RewriteRule (.*) https://{{ host.vhost }}$1 [R,L]
    </VirtualHost>
{% endfor %}
